{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Settings": {
    "Ollama": {
      "BaseUrl": "http://localhost:11434",
      "TimeoutSeconds": 600,
      "OcrPrompt": "This is a Brazilian handwritten essay form (folha de redação). Ignore all printed text, form fields, headers, barcodes, and instructions. Extract only the handwritten text written in the lined area. Return the handwritten content exactly as written, preserving line breaks."
    }
  }
}
